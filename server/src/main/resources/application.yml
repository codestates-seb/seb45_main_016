spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyComponentPathImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: b05b33f9ad8c9790f4281e18ebd1e7fc
            client-secret: VzgmpqYMMMbgXPq9HXPRQUhUtOJRUWcY
            redirect-uri: http://localhost:3000/login/oauth/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: POST
            client-name: Kakao
            scope:
              - profile_nickname
              - profile_image
              - account_email
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
    properties:
      hibernate:
        format_sql: true
logging:
#  security:
#    oauth2:
#      client:
#        registration:
#          kakao:
#            client-id: # ? ?????? >? ?? > ? ? > REST API ?
#            client-secret:  # ? ?????? > ?? ?? > ??? ??? > ?? > Client Secret
#            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
#            authorization-grant-type: authorization_code
#            client-authentication-method: POST
#            client-name: Kakao
#            scope:
#              - profile_nickname
#              - profile_image
#              - account_email
#              - talk message
#        provider:
#          kakao:
#            authorization-uri: https://kauth.kakao.com/oauth/authorize
#            token-uri: https://kauth.kakao.com/oauth/token
#            user-info-uri: https://kapi.kakao.com/v2/user/me
#            user-name-attribute: id
  level:
    org:
      springframework:
        orm:
          jpa: debug
secret:
  key: "=hN6HzankT%2Bi7Qds9PJjAjFb6R6xzw5DAvqBHMSOjw4yaA98ubFd6IRzWT8Kz5CcCiksKJz7qPFdZOsOcjjn4MA%3D%3D"
mail:
  address:
    admin: admin@gmail.com # admin:'${ADMIN_EMAIL}' 나중에 변경하기
jwt:
  key : dlfeksgo16@dlfeksgo16@dlfeksgo16@goqhwk16@ #  key: '${JWT_SECRET_KEY}' 나중에 변경하기
  access-token-expiration-minutes: 60
  refresh-token-expiration-minutes: 420